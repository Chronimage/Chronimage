# Checkpoint · 2026-04-19 20:55 UTC · Phase 1 — Deep AI Catalog (week 1)

## Branch
`feature/phase1-live-data` · PR #13 open against `develop` · all 13 CI checks green

## Just finished (this session)

- **Google Photos Takeout ingest** (`src-tauri/src/import/google_takeout.rs`)
  - Reads `<photo>.json` sidecars after the core pipeline runs
  - Enriches `captured_at` + `gps_lat` / `gps_lng` via COALESCE (embedded EXIF always wins)
  - Exposed as `import_google_takeout` Tauri command
  - OnboardScreen "Google Photos" button is now functional (folder picker → create source → run pipeline + sidecar pass)

- **iCloud-for-Windows detection** (`src-tauri/src/import/icloud.rs`)
  - `detect_icloud_path()` checks `%USERPROFILE%\Pictures\iCloud Photos\Photos` then `Downloads` then root
  - `detect_icloud_path` Tauri command; OnboardScreen iCloud button auto-detects and shows "(detected)" label
  - If not detected, falls back to a manual folder picker

- **iPhone USB enumeration** (`src-tauri/src/import/iphone_usb.rs`)
  - `IPortableDeviceManager::GetDevices` via Windows `Win32_Devices_PortableDevices` feature
  - Filters to Apple-manufactured devices (manufacturer string contains "apple")
  - Non-Windows stub returns `[]`
  - OnboardScreen shows one functional button per connected iPhone; disabled placeholder when none connected
  - File transfer deferred — this session only enumerates devices

- **Three new Tauri commands + TS wrappers**
  - `import_google_takeout`, `detect_icloud_path`, `list_iphone_devices`
  - `importGoogleTakeout`, `detectIcloudPath`, `listIphoneDevices` in `src/tauri/invoke.ts`
  - `useImportGoogleTakeout`, `useDetectIcloudPath`, `useIphoneDevices` in `src/state/queries.ts`

- **Coverage fixed** — vitest threshold was failing at 44.8% (threshold 50%)
  - Added `ResizeObserver` + `@tauri-apps/plugin-dialog` stubs to `tests/setup.ts`
  - New test files: `src/screens/OnboardScreen.test.tsx` (6 tests), `src/screens/catalog/CatalogScreen.test.tsx` (4 tests)
  - Added hook tests for `useRefreshSmartAlbums`, `useImportGoogleTakeout`, `useDetectIcloudPath`, `useIphoneDevices`
  - Coverage now at 59% — 9 points above threshold

- **CI fully green on PR #13** — 13/13 checks pass including `Rust clippy + test (Windows)` and `Tauri dev-build smoke (Windows)`

## What is functional end-to-end (cumulative)

### Onboarding / sources
- Add local folder → creates source → live import pipeline → progress bar with ETA → finished toast → "Open Catalog"
- Google Photos Takeout folder → same pipeline + sidecar enrichment pass for timestamps/GPS
- iCloud-for-Windows → auto-detected or manual picker → standard pipeline
- iPhone USB → per-device button when connected → manual DCIM folder picker → standard pipeline
- Cleanup dry-run: shows per-source reclaimable GB (SHA256-verified, sole-copy protected); "Clean up" button stubbed

### Catalog
- Virtual photo grid (ResizeObserver column count, @tanstack/react-virtual row virtualizer, 500-photo window)
- Album filter: side panel album selection filters grid via smart album rule evaluation
- Detail overlay: double-click → full-screen view with filmstrip, EXIF, AI tag chips, arrow-key nav, Escape
- Rediscovery rows above grid: "ON THIS DAY" + "UNSEEN · WORTH ANOTHER LOOK" (hidden when empty)
- Smart album rule engine (`catalog/rules.rs`): tag, cluster, exif, quality, is_raw rules with SQL injection whitelist

### Rust backend
- Import pipeline: scan → SHA256 hash → insert photos + source_copies → detect RAW+JPG pairs → emit progress events
- Smart album counts refresh automatically after each import
- `on_this_day`, `unseen_photos`, `cleanup_dry_run`, `refresh_smart_albums` commands
- 12 seeded system smart albums (Portraits, Golden Hour, Night, etc.)
- 77 Rust tests, 53 TS tests

## Open threads

- **`cleanup_execute`** — two-step deletion with confirm token + SHA256 re-verify + 2× free-space check. Safety gates mandatory before implementing. The dry-run plan_id must become the confirm token.
- **5-step onboarding stepper** — design has Welcome · Sources · Import · Models · People-naming stepper; current OnboardScreen is a single functional page
- **AI layer** — nothing yet: `ai/budget.rs` (VRAM detect), `ai/siglip.rs` (embeddings), `ai/aesthetic.rs` (NIMA), `ai/faces.rs`, `ai/cluster.rs` — spawn `ai-wrangler` agent for this
- **Search** — search bar renders but is a no-op; needs SigLIP text encoder → sqlite-vec k-NN
- **Dedupe** — `dedupe/phash.rs` + SigLIP cosine not yet started
- **NAS connector** — disabled "NAS" button in OnboardScreen
- **iPhone file transfer** — enumeration done; actual WPD MTP file copy deferred
- **Google Photos OAuth (live sync)** — only Takeout (static export) is implemented; OAuth live API is separate
- **First-run model downloader** — `models/` gitignored; no download surface yet
- **`src-tauri/Cargo.lock`** — not committed to git (confirm intentional or add to repo)
- **`tauri.conf.json` updater pubkey** — still placeholder; must be generated before release workflow

## Next session start

1. Merge PR #13 into `develop` (all CI green)
2. Create `feature/phase1-ai` off `develop`; spawn `ai-wrangler` agent to scaffold `src-tauri/src/ai/budget.rs` (VRAM detection via DXGI) + `ai/siglip.rs` (ONNX Runtime CPU path first)
3. Or, if UX is the priority: port the 5-step onboarding stepper from `design-handoff/chronimage/` using `ux-porter` agent

## Notes

- All three new source connector modules have a non-Windows stub (`cfg(not(windows))`) returning `Ok([])`; safe for CI runners on Linux
- Sidecar enrichment uses `COALESCE` so it never overwrites EXIF-extracted `captured_at` — safe to run on any import root even if it isn't actually a Takeout export
- `Win32_Devices_PortableDevices` + `Win32_UI_Shell_PropertiesSystem` added to Cargo.toml windows features
- Coverage threshold enforcement is now healthy (59%); the two new test files (`OnboardScreen.test.tsx`, `CatalogScreen.test.tsx`) are the key additions — don't remove them
