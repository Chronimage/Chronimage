# Chronimage context bundle · 2026-04-19 09:00 UTC · phase1-scaffold

> Self-contained handoff. Another Claude Code session can `/context-load` this file and resume work with no prior conversation context. Everything below is a snapshot at the dump time — always re-check the live repo state before acting.

## 1. Current phase marker
**Current phase:** Phase 1 — Deep AI Catalog (week 1 · scaffolding landed, feature implementation next)

## 2. Working directory & branch
- path: `C:\Users\jayas\OneDrive\Documents\GitHub\halide`
- branch: `master` (no upstream; intend to create `develop` + `main` at first real commit)
- upstream: (none)
- head: (no commits yet — repo was empty `.git` at session start; everything is uncommitted)
- working tree: 27 files / dirs untracked (all new this session)

## 3. Resume protocol (verbatim from CLAUDE.md)

1. Read `CLAUDE.md` (you just did).
2. `cat docs/checkpoints/latest.md` — contains: what was just done, what's open, exact next action.
3. Open the active PRD: `docs/prds/phase-0.md` for Phase 0, `phase-1.md` once Phase 0 exits.
4. Check `git status` + `git log -5` for uncommitted / recent work.
5. Run `pnpm typecheck && cargo check --manifest-path src-tauri/Cargo.toml` to confirm the baseline is green before starting.

At session end: run `/phase-checkpoint` to update `docs/checkpoints/latest.md` + flip the phase marker at the top of this file if appropriate.

### Cross-session / cross-machine handoff (new this session)
- `/context-dump [label]` — writes a self-contained bundle at `docs/context-bundles/YYYY-MM-DD-HHMM[-label].md`.
- `/context-load <path>` — reads a bundle in a fresh session, cross-checks against the live repo.

## 4. Tech stack (verbatim from CLAUDE.md)

- **Shell:** Tauri v2 (Rust host + WebView2)
- **Frontend:** React 19 + TypeScript + Vite + Tailwind v4 + Radix UI + Zustand + TanStack Router + TanStack Query + @tanstack/react-virtual
- **Backend:** Rust · tokio · rayon · sqlx (SQLite + sqlite-vec) · rawler · libheif-rs · image-rs · wgpu · ort (ONNX Runtime)
- **AI:** SigLIP-B (embeddings, CPU-fast), ArcFace+RetinaFace via FaceONNX (faces), gemma4-9b via llama.cpp sidecar (captions, GPU-opt), HDBSCAN (face clustering), pHash + SigLIP cosine (dedupe)
- **Testing:** Vitest + RTL + Playwright + tauri-driver + axe-core + criterion + proptest + cargo-fuzz + cargo-mutants
- **Quality:** lefthook (pre-commit) + biome + cargo fmt/clippy + commitlint + cargo-deny + cargo-audit
- **Release:** 4 channels (stable / beta / nightly / insider) via GitHub Actions; signed MSI via Tauri v2 + EV cert; auto-update via `tauri-plugin-updater`

Subagent model preference: **Sonnet** for all subagent spawns. Every `.claude/agents/*.md` sets `model: sonnet` in frontmatter.

## 5. Rules (verbatim from CLAUDE.md)

### Rust
- No `unwrap()` / `expect()` / `panic!()` outside `#[cfg(test)]`. Use `Result` + `?` + `thiserror` error types.
- No `dbg!()` in committed code.
- `cargo fmt` + `cargo clippy --all-targets -- -D warnings` must be clean.
- Tests live next to modules (`mod tests { … }`) for unit, `src-tauri/tests/` for integration.
- Every `#[tauri::command]` gets ≥ 2 tests (happy + edge).

### TypeScript
- No `any` in committed code. `unknown` + type narrowing is fine.
- No `console.log` (use `debug`/`info`/`warn`/`error` from `src/util/log.ts`).
- `pnpm typecheck` must pass.

### Commits
- Conventional Commits enforced by commitlint: `feat(catalog): …`, `fix(import): …`, `perf(raw): …`.
- Signed commits required on `main` and `develop`.
- No commits to `main` or `develop` directly — PRs only.
- Branches: `feature/xyz` off `develop`, `hotfix/xyz` off `main`.

### Security / privacy
- No network calls from Rust without an explicit user-triggered flow.
- No telemetry in v1. Call sites exist (`telemetry::event(…)`) but no-op.
- Source-side deletion is always two-step + SHA256-gated + ≥2× free-space-gated.

## 6. Active PRD summary
Path: `docs/prds/phase-1.md`
Goal: "Import, tag, face-cluster, dedupe (incl. RAW+JPG pairs), natural-language search, source-side cleanup, rediscovery — end-to-end on 200k+ photos, without OOMing, on a no-GPU laptop."
Open deliverables: ~14 top-level items; scaffolding landed this session, no exit criteria met yet.
Exit criteria: 8 items, all testable (bound to test files named `tests/e2e/phase-1-*.spec.ts` and `src-tauri/tests/phase_1_*.rs`).

**PRD read from `docs/prds/phase-1.md` — full contents on disk; re-read directly rather than inlining here (too large for bundle — 200+ lines).**

## 7. Latest checkpoint

Path: `docs/checkpoints/latest.md`
See `docs/checkpoints/latest.md` for the full checkpoint, including:
- Phase 0 exit confirmation
- Phase 1 scaffolding summary (SQL migration, Rust catalog/import modules, frontend primitives/screens, fixtures)
- 32 Rust tests + 6 frontend tests all green
- Open threads ordered by priority (sha2 hashing → AppState → smart album seeder → import pipeline → source connectors → source cleanup → AI layer)
- Next-session-start sequence (verification gauntlet + item #1)

The checkpoint file on disk is the authoritative copy.

## 8. Memory index

Memory lives at `C:\Users\jayas\.claude\projects\C--Users-jayas-OneDrive-Documents-GitHub-halide\memory\`:

- `MEMORY.md` — index
- `user_profile.md` — Jay, Sony A7 IV hobbyist, library fragmented across cloud/local, refuses storage lock-in and subscriptions
- `feedback_subagent_model.md` — use Sonnet (not Opus) for subagents; user preference confirmed 2026-04-19
- `project_chronimage.md` — Windows desktop AI photo organizer, Tauri v2 + Rust + React, 6-phase roadmap, plan file at `~/.claude/plans/fetch-this-design-file-virtual-donut.md`

## 9. Recent git history
```
(no commits yet — repo was initialized empty; all work from this session is uncommitted)
```

## 10. Uncommitted changes (first 20 untracked)
```
?? .claude/
?? .gitattributes
?? .github/
?? .gitignore
?? CLAUDE.md
?? LICENSE
?? README.md
?? biome.json
?? cargo-deny.toml
?? cliff.toml
?? commitlint.config.js
?? design-handoff/
?? docs/
?? index.html
?? lefthook.yml
?? models/
?? package.json
?? playwright.config.ts
?? pnpm-lock.yaml
?? rust-toolchain.toml
?? scripts/
?? src-tauri/
?? src/
?? tailwind.config.ts
?? tests/
?? tsconfig.json
?? vite.config.ts
?? vitest.config.ts
```

(There are 27 top-level entries. All of `.claude/`, `.github/`, `docs/`, `scripts/`, `src/`, `src-tauri/`, `tests/` contain many files — commit them grouped logically on the next session's first commit. Suggested first commit series below.)

## 11. Open TODOs in tracked source
None committed yet. No `TODO(cc)` or `TODO(blocker)` markers present in this session's code.

## 12. Verification commands

Run these to confirm baseline is green before starting new work in a fresh session:

```bash
# Rust toolchain must be on PATH
export PATH="$HOME/.cargo/bin:$PATH"

# Install
pnpm install --frozen-lockfile

# Frontend
pnpm typecheck
pnpm exec biome check .
pnpm test

# Rust
cargo check --manifest-path src-tauri/Cargo.toml
cargo clippy --manifest-path src-tauri/Cargo.toml --all-targets -- -D warnings
cargo test --manifest-path src-tauri/Cargo.toml --lib

# Dev server (optional smoke)
pnpm dev  # http://127.0.0.1:1420/
```

Expected: all green. 6 frontend tests + 32 Rust tests pass.

## 13. Next concrete action

Priority order for the next session (full list in `docs/checkpoints/latest.md`):

1. **Install Rust on PATH for the shell** — `export PATH="$HOME/.cargo/bin:$PATH"` (add to `.bashrc` / Windows-PATH permanently later).
2. **Run the verification gauntlet** above — confirm baseline is green before touching anything.
3. **sha2 hashing** — add `sha2 = "0.10"` to `src-tauri/Cargo.toml`, replace the placeholder in `src-tauri/src/import/hash.rs` with a real streaming SHA256 using `sha2::Sha256::new()` + `digest.update(&buf[..n])` + `hex::encode(digest.finalize())`. Add `hex = "0.4"` too. Keep the same function signature. Update the test to assert known-vector output for `b"hello"` → `2cf24dba5fb0a30e26e83b2ac5b9e29e1b161e5c1fa7425e73043362938b9824`.
4. **AppState with SqlitePool** — create `src-tauri/src/state.rs` exposing `pub struct AppState { pub pool: SqlitePool }`. In `main.rs` setup closure, `app.manage(AppState { pool: chronimage::catalog::open_pool(PoolOptions::new(catalog_db_path()?)).await? })`. Convert `import_dry_run` to take `State<'_, AppState>` as a no-op demonstration.
5. **Smart album seeder** — `src-tauri/src/catalog/seed.rs` with `seed_default_smart_albums(&pool)` that inserts the 12 ALBUMS from the design on first launch (detect via `SELECT COUNT(*) FROM smart_albums WHERE is_system = 1`).
6. **First commit series** (suggested, Conventional Commits):
   - `chore(repo): phase 0 scaffold (hooks, CI, configs, design handoff)`
   - `feat(repo): CLAUDE.md + agent infrastructure`
   - `feat(catalog): phase 1 schema + sqlx pool`
   - `feat(import): scanner + RAW+JPG pair detector + streaming hasher`
   - `feat(ui): chrome shell + primitives + catalog screen`
   - `feat(ci): 7 workflows + dependabot + CODEOWNERS`
   Each on a `feature/` branch, PR to `develop`.

Then continue through `docs/checkpoints/latest.md` § Open threads.

## 14. Notes

- Plan file (original, user-approved): `~/.claude/plans/fetch-this-design-file-virtual-donut.md`. Authoritative source for roadmap & rationale. Don't edit it unless the user explicitly requests a plan revision.
- Repo directory is `halide/` for historical reasons (Halide was the design's placeholder name, trademarked by Lux Optics). Product name is **Chronimage** everywhere in code/bundles/copy.
- OneDrive path — file watching can be flaky; `.gitignore` excludes `*~` / `.~*` temp files.
- Rust toolchain pinned to `stable` (1.88+ needed for transitive deps). Tauri edition2024 requirement drove this.
- Placeholder icon set `src-tauri/icons/*.png + .ico` — solid mint. Replace with real art before v0.2.0.
- `tauri.conf.json` has placeholder updater pubkey — generate real one with `pnpm exec tauri signer generate` before first release workflow runs.
- All 4 Tauri channel configs exist: `tauri.conf.json` (stable, default), `tauri.beta.conf.json`, `tauri.nightly.conf.json`, `tauri.insider.conf.json`. Each overrides identifier + productName + updater endpoint.
- GitHub Actions: 7 workflows configured but none have run (no upstream yet). CI will need `CODECOV_TOKEN`, `TAURI_UPDATER_PRIVATE_KEY`, `WINDOWS_SIGN_CERT_PFX_B64` secrets on first push.
- `Entitlements::current()` returns all-true in v1. Gate-ready scaffolding for future monetization tiers — don't add pricing yet (user's explicit ask).
- `scripts/*.cjs` + `.claude/hooks/*.cjs` intentionally use CommonJS (`require`) because root `package.json` is `type: module`. Don't rename back to `.js`.
- sqlx migrations are already applied automatically by `open_pool` — no need to run `pnpm migrate:run` manually.
- TanStack Router is installed but not wired. When adding in a later Phase 1 session, re-enable the `TanStackRouterVite` plugin in `vite.config.ts` AND create `src/routes/__root.tsx` + per-screen routes first.
