# Checkpoint 2026-04-20 11:30 UTC · Phase 1 — Deep AI Catalog (week 1)

## Just finished
- Merged 3 parallel worktree branches into `feature/phase1-ai`:
  - `worktree-agent-a54234dd` — `feat(dedupe)`: cosine similarity grouping via SigLIP embeddings (`src-tauri/src/dedupe/confirm.rs`, `dedupe/phash.rs`)
  - `worktree-agent-a5a2e8e3` — `feat(catalog)`: sqlite-vec migration + NL search command (`src-tauri/migrations/20260421000000_sqlite_vec.sql`, `catalog/db.rs`, `commands.rs` `search_photos`, `src/main.tsx` QueryClientProvider)
  - `worktree-agent-a573ac82` — `feat(catalog)`: `cleanup_execute` two-step deletion with SHA256 re-verify + 2× free-space gate (`commands.rs`, `state/queries.ts`, `tauri/invoke.ts`, `screens/OnboardScreen.tsx`)
- Resolved all merge conflicts (add/add in `dedupe/mod.rs`; content conflicts in `commands.rs`, `main.rs`, `queries.ts`, `invoke.ts`, `CatalogScreen.tsx`, `OnboardScreen.tsx`)
- Fixed structural mismatch: old `CleanupPlan[]` per-source API updated to new single-plan `CleanupPlan { plan_id, confirm_token, sources[] }` across `invoke.ts`, `queries.ts`, `OnboardScreen.tsx`
- Changed `useCleanupDryRun` from `useQuery` → `useMutation` (correct: generates single-use confirm tokens)
- Fixed `invoke.test.ts` to remove stale `cleanupDryRun(sourceId)` arg call (new signature is no-arg)
- All 3 commits landed clean through lefthook (biome, typecheck, clippy, commitlint)

## Open threads
- **Worktree branch cleanup blocked**: `worktree-agent-a5a2e8e3` and `worktree-agent-a573ac82` branches still exist — worktree dirs at `.claude/worktrees/` couldn't be deleted (OneDrive permission issue). Clean up manually: `git worktree remove .claude/worktrees/agent-a5a2e8e3 && git worktree remove .claude/worktrees/agent-a573ac82 && git branch -d worktree-agent-a5a2e8e3 worktree-agent-a573ac82`
- **`ai/faces.rs` scaffold not started** (PRD §5 item 2: RetinaFace detect + ArcFace embed) — was item 5 in `docs/next-session.md`, skipped when launching parallel agents
- **`ai/cluster.rs` HDBSCAN** not started (PRD §5 item 3)
- **`ai/caption.rs` llama.cpp sidecar** not started (PRD §5 item 4, GPU-only path)
- **Lift & Shift** (`commands/lift_and_shift.rs`) not started (PRD §11)
- **Face clustering UI** not started (PRD §10)
- **Smart album rule engine** not started (PRD §7 — seed albums exist but rule JSON evaluator is stub)
- **Rediscovery "first new camera" + "unflagged favorites"** smart album rules not wired (PRD §13)
- **Settings screen** not started (PRD §14)
- **Biome nursery warnings in `OnboardScreen.tsx`**: 4 `useSortedClasses` warnings (lines 211, 236, 1179) — non-blocking but `biome check --write --unsafe` would fix them

## Next session start
1. Clean up lingering worktree branches: `git worktree list` then remove stale entries + `git branch -d worktree-agent-a5a2e8e3 worktree-agent-a573ac82`
2. Spawn `ai-wrangler` agent to scaffold `src-tauri/src/ai/faces.rs` (RetinaFace detect + ArcFace embed stubs with ONNX Runtime wiring) and `ai/cluster.rs` (HDBSCAN over ArcFace embeddings, stable cluster IDs)

## Notes
- `useCleanupDryRun` must remain a `useMutation` — it generates a single-use `confirm_token` stored in `PENDING_PLANS` in-memory map; calling it twice generates two separate plan IDs
- `cleanupDryRun()` in `invoke.ts` takes no arguments (no per-source scoping in v1 — always plans across all sources)
- sqlite-vec init is best-effort via `try_init_sqlite_vec()` in `catalog/db.rs`; NL search gracefully returns empty array if extension not loaded
- The 4 biome `useSortedClasses` warnings in `OnboardScreen.tsx` are nursery-rule warnings only; they don't block CI (biome exits 0 with warnings)
- Phase 1 exit criteria still have substantial work remaining (faces, clustering, lift-and-shift, settings, face UI) — do not advance phase marker yet
